<?php
<?php
// This script was a helper to import .sql files via the app\core\Db class.
// The project is configured to use the database declared in `app/core/Db.php`.
// Per repository policy, do NOT import SQL files from the `sql/` folder.
// To avoid accidental usage, this script is intentionally disabled.

echo "This import script is disabled. The application should use the DB configured in app/core/Db.php\n";
exit(0);
